import styled from "styled-components";

export const StyledButtonSingOut = styled.button`
  position: relative;
  width: 30px;
  height: 30px;

  svg {
    transform: scale(1.8);
    color: #bdbdbd;
  }

`;
