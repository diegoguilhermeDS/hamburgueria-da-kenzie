import styled from "styled-components";

export const StyledButtonSingOut = styled.button`
  position: relative;
  width: 1.875rem;
  height: 1.875rem;

  svg {
    transform: scale(1.8);
    color: #bdbdbd;
  }
`;
